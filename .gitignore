node_modules
.next
.next-stale-*
out
.env
.env.local
.secrets/
# Local MCP server config — machine/account specific, and its URL pins one
# Supabase project ref. Each dev adds their own with `claude mcp add`.
.mcp.json
*-credentials*.json
*service-account*.json
*-*-*.json
!package-lock.json
!tsconfig*.json
!postcss.config.json
# Local run output: verbatim backups and baseline timing artifacts. The stamped
# filenames already match *-*-*.json, but latest.json does not.
backups/
dev.db
dev.db-journal
prisma/migrations
public/generations/*
!public/generations/.gitkeep
public/uploads/*
!public/uploads/.gitkeep
.DS_Store
~$*.xlsx
*.tsbuildinfo
.tsx-cache
